experiment: e2b
backbone: resnet50
fpn: true
imgsz_max: 800
epochs: 20
batch_size: 8  # frozen backbone = low VRAM; A100-40GB handles batch=8 easily
optimizer: SGD
lr: 0.005
momentum: 0.9
weight_decay: 0.0001
warmup_epochs: 1
lr_milestones: [12, 16]
lr_gamma: 0.1
patience: 7
seed: 42
device: auto
